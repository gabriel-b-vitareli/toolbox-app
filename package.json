{
  "name": "toolbox",
  "version": "0.4.0",
  "description": "Central de utilidades - conversores, calculadoras, geradores e ferramentas do dispositivo em um so app.",
  "scripts": {
    "start": "npx serve www",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
